venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

.git/
.gitignore

.env

# Runtime state — provided by mounted volumes, never baked into the image.
chroma_db/
data/
datasets/

# Note: database/ is a Python package the app imports. Do not exclude it.
# Only the SQLite files it manages are excluded, via data/.

*.log

.ipynb_checkpoints/

models/
*.bin
*.pt

.streamlit/

# The frontend deploys separately to Vercel.
frontend/

# Evaluation fixtures and generated reports are not needed at runtime.
evaluation/results/
evaluation/real_pdfs/
test_*.py

.DS_Store
